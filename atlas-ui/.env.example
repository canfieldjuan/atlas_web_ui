# Optional: Override the WebSocket URL used to connect to the Atlas backend.
# When unset, the UI auto-detects the URL from the browser's current hostname.
#
# Examples:
#   VITE_WS_URL=ws://192.168.1.50:8000/api/v1/ws/orchestrated
#   VITE_WS_URL=wss://atlas.example.com/api/v1/ws/orchestrated
#
# VITE_WS_URL=
